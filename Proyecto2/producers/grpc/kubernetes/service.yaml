apiVersion: v1
kind: Service
metadata:
  name: grcp-client-service
  namespace: so1-proyecto2
spec:
  selector:
    app: grcp-producer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP