apiVersion: apps/v1
kind: Deployment
metadata:
  name: wasm-producer
  namespace: so1-proyecto2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wasm-producer
  template:
    metadata:
      labels:
        app: wasm-producer
    spec:
      containers:
      - name: wasm-producer
        image: alvarog1318/s1p2_wasm_producer:v0.2
        ports:
        - containerPort: 3000
        resources:
          limits:
            cpu: "0.3"
            memory: "500Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: wasm-producer-service
  namespace: so1-proyecto2
spec:
  selector:
    app: wasm-producer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP